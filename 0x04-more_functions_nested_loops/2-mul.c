#include "holberton.h"
#include <stdio.h>
/**
 * mul - Function that checks for uppercase character.
 * @k: Check
 * @c: Check
 * Return: 1 if int == digit and 0 else.
 */

int mul(int k, int c)
{
return (k * c);
}
